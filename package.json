{
  "name": "basic-nodejs-server",
  "version": "1.0.0",
  "description": "In this project, I have  build a basic web server using Node.js and Express.js. The server will handle multiple HTTP requests, serve static content, and allow basic CRUD operations (Create, Read, Update, Delete) on a simple dataset (e.g., users or products).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.3",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
